<% provide :page_title, 'Hotels' %>
<% currently_at 'Hotels' %>

<div class="row mb-3">
  <div class="col-12">
    <h1>All hotels
      <%= link_to 'Create', new_hotel_path(@deed), class: 'btn btn-primary btn-sm'%>
      
    </h1>
  </div>
</div>


<%= pagination @pagy %>

<%= render @hotels %>

<%= pagination @pagy %>